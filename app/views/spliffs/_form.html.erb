<%= form_for(spliff) do |f| %>
  <% if spliff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(spliff.errors.count, "error") %> prohibited this spliff from being saved:</h2>

      <ul>
      <% spliff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
