<%= form_for(morning_ritual) do |f| %>
  <% if morning_ritual.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(morning_ritual.errors.count, "error") %> prohibited this morning_ritual from being saved:</h2>

      <ul>
      <% morning_ritual.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :day %>
    <%= f.date_select :day %>
  </div>

  <div class="field">
    <%= f.label :waketime %>
    <%= f.datetime_select :waketime %>
  </div>

  <div class="field">
    <%= f.label :exercise %>
    <%= f.check_box :exercise %>
  </div>

  <div class="field">
    <%= f.label :brush %>
    <%= f.check_box :brush %>
  </div>

  <div class="field">
    <%= f.label :protein %>
    <%= f.check_box :protein %>
  </div>

  <div class="field">
    <%= f.label :journal %>
    <%= f.check_box :journal %>
  </div>

  <div class="field">
    <%= f.label :drink_water %>
    <%= f.check_box :drink_water %>
  </div>

  <div class="field">
    <%= f.label :read %>
    <%= f.check_box :read %>
  </div>

  <div class="field">
    <%= f.label :meditate %>
    <%= f.check_box :meditate %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
